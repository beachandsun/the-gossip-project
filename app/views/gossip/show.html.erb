<h1>Gossip</h1>


<div class="card">
  <div class="card-header">
   <%= @gossip.title %>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
<p><%= @gossip.content %>
<footer class="blockquote-footer"><%= link_to "#{@author.first_name}", author_path(@author.id) %><cite title="Source Title"> <%= @gossip.updated_at %></cite></footer></p>
    </blockquote>
  </div>
</div>